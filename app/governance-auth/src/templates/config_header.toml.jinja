{#- Header for the per-user config file, written only when creating it. The
     trailing blank line separates it from the first key.
-#}
# Written by `governance-auth` (login or configure -- both persist what
# they resolved). Edit freely: later runs update only the keys they set and
# leave the rest, comments included.
# Secrets are NOT stored here; see `otel_token_file` in the docs.

