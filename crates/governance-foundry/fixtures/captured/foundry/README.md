Drop captured Microsoft Foundry hosted-agent OTLP fixtures here. See `../README.md` for the
procedure. This file is a placeholder only (the harness ignores anything not ending in
`.json`); it exists purely so the directory is tracked by git before a real capture lands.
