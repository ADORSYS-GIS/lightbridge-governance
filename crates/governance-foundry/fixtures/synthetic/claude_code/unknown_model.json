{
  "_fixture_meta": {
    "label": "synthetic",
    "provenance": "hand-authored from the documented attribute contract (RFC-0002; claude_code.rs's module doc) -- NOT captured from a real Claude Code OTLP export. Encodes our ASSUMPTIONS about the wire format, not verified reality. See fixtures/README.md.",
    "pins": "model.name names a model this platform has never priced -- the normalizer must pass the string through unmodified and must NOT reject, substitute a known model, or otherwise special-case it. Pricing that model as 'unknown, not zero' is governance-core::calculate_model_cost's job (see missing_pricing_is_stored_as_unknown_not_zero in crates/governance-core/src/ingest.rs), which this normalizer-only harness cannot exercise -- this fixture only pins the normalizer's half of that contract: it does not touch or judge the model name."
  },
  "resourceSpans": [
    {
      "resource": {
        "attributes": [
          { "key": "service.name", "value": { "stringValue": "claude-code" } },
          { "key": "user.email", "value": { "stringValue": "dev@example.com" } }
        ]
      },
      "scopeSpans": [
        {
          "spans": [
            {
              "traceId": "cc-trace-0004",
              "spanId": "cc-span-0004",
              "startTimeUnixNano": "1706000000000000000",
              "endTimeUnixNano": "1706000004200000000",
              "attributes": [
                { "key": "session.id", "value": { "stringValue": "cc-session-04" } },
                { "key": "model.name", "value": { "stringValue": "claude-vNext-unreleased-preview" } },
                { "key": "tokens.input", "value": { "intValue": "500" } },
                { "key": "tokens.output", "value": { "intValue": "120" } }
              ],
              "events": []
            }
          ]
        }
      ]
    }
  ]
}
