{{- /*
Body shared with the OpenCode collector's policy -- see
`lightbridge-governance.publicOtelNetworkPolicy` in _helpers.tpl, which carries
the full "why there is no `egress:` section" incident writeup
(lightbridge-governance#85/#87/#88/#89). Read it before adding one back.
*/}}
{{- if and .Values.aiCliOtel.enabled .Values.networkPolicy.enabled }}
{{- include "lightbridge-governance.publicOtelNetworkPolicy" (dict "root" . "otel" .Values.aiCliOtel "name" (include "lightbridge-governance.aiCliOtelName" .)) }}
{{- end }}
