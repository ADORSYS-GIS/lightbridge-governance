{{- /*
Body shared with the OpenCode collector's ingress -- see
`lightbridge-governance.publicOtelIngress` in _helpers.tpl.
*/}}
{{- if and .Values.aiCliOtel.enabled .Values.aiCliOtel.ingress.enabled }}
{{- include "lightbridge-governance.publicOtelIngress" (dict "root" . "otel" .Values.aiCliOtel "name" (include "lightbridge-governance.aiCliOtelName" .)) }}
{{- end }}
